{{!-- <li class="list-group-item">
    {{burger_name}}
    {{#if devour}}
    {{else}}
    <button class="change-devoured btn btn-primary btn-sm" data-id="{{id}}" data-newdevoured="{{devour}}">
        DEVOUR IT!!
    </button>
    {{/if}}
</li> --}}

{{#if devour}}
<li class="list-group-item disabled py-2">
    {{burger_name}}
</li>
{{else}}
<li class="list-group-item py-2">
    {{burger_name}}
    <button class="change-devoured btn btn-primary btn-sm py-0" data-id="{{id}}" data-newdevoured="{{devour}}">
        DEVOUR IT!!
    </button>
</li>
{{/if}}


{{!-- <li>
    {{burger_name}}

    <button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devour}}">
        {{#if devour}}DEVOURED!{{else}}DEVOUR IT!!{{/if}}
    </button>
</li> --}}